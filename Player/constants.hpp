#pragma once

#include <string>
#include <glib.h>

const std::string LOGGER = "logger";

// FIXME remove from constants.hpp
struct Point
{
    int left;
    int top;
};

// FIXME remove from constants.hpp
struct Size
{
    int width;
    int height;
};
