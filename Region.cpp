#include "Region.hpp"

Region::Region(int id, int width, int height, int top, int left, int zindex, bool loop) :
    id(id), width(width), height(height), top(top), left(left), zindex(zindex), loop(loop)
{

}
